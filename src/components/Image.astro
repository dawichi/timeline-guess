---
interface Props {
	src: string
}

const { src } = Astro.props;
---
<div class="rounded bg-contain bg-no-repeat bg-center h-full w-full overflow-hidden" style={`background-image: url(${src});`} />
