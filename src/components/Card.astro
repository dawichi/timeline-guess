---
const { title, year, image } = Astro.props
---
<div class="bg-zinc-700 w-80 p-4 mx-auto rounded shadow">
    <h2 class="text-xl font-bold">{String(title)}</h2>
    <p>{String(year)}</p>
    <img src={`/images/cards/${image}`} class="w-full rounded" />
</div>
