---
import Card from '../components/Card.astro';
import Layout from '../layouts/Layout.astro'

import { game } from '../scripts/game'

// const cardDisplayed = game.getRandomCard()

// console.log(game.getRandomCard())
---

<Layout>
    <div class="container mx-auto grid gap-2">
        {game.placedCards.map(card => (
            <Card {card} />
        ))}
    </div>

    <pre class="bg-zinc-800">
        {JSON.stringify(game.placedCards)}
        <!-- {cardDisplayed.title} -->
    </pre>
</Layout>
